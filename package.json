{
  "name": "custom-document-link-rules",
  "publisher": "ayutaya",
  "displayName": "Custom Document Link Rules",
  "description": "Turn text matched by your own regular expressions into clickable Document Links, for any language.",
  "version": "0.0.1",
  "repository": "https://github.com/knaka/custom-document-link-rules",
  "engines": {
    "vscode": "^1.138.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "document link",
    "related files",
    "regex",
    "custom",
    "link"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "customDocumentLinkRules.openFile",
        "title": "Open File on Line and Char position",
        "category": "Custom Document Link Rules"
      }
    ],
    "configuration": {
      "title": "Custom Document Link Rules",
      "properties": {
        "custom-document-link-rules.rules": {
          "type": "array",
          "default": [],
          "scope": "resource",
          "description": "Rules that turn matched text into Document Links (regexes)",
          "items": {
            "type": [
              "string",
              "object"
            ],
            "description": "If string, first capturing group is used",
            "properties": {
              "pattern": {
                "type": "string",
                "description": "Find the text pattern containing a linked file (regex)"
              },
              "filePath": {
                "type": "string",
                "description": "Construct the linked file path, use any capture group from 'find' regex. Use \"/\" as first character to signal that file path is relative to a fileroot path",
                "default": "$1"
              },
              "lineNr": {
                "type": "string",
                "description": "To which line number do you want to jump, use any capture group from 'find' regex and/or an expression using the current match position. No default"
              },
              "searchText": {
                "type": "string",
                "description": "To which line do you want to jump, search for the literal string, use any capture group from 'find' regex. No default"
              },
              "searchTextIsExpression": {
                "type": "boolean",
                "description": "Evaluate 'searchText' as a JavaScript Expression, capture groups from 'find' regex are available as $1, ... and/or use the current match position. default: false"
              },
              "charPos": {
                "type": "string",
                "description": "To which character position do you want to jump, use any capture group from 'find' regex and/or an expression using the current match position. Only used when 'lineNr' is defined. No default"
              },
              "rangeGroup": {
                "type": "string",
                "description": "The capture group that is the range for the Document Link. Use $n notation. Default: if no 'lineNr' specified uses capture group from 'filePath'."
              },
              "isAbsolutePath": {
                "type": "boolean",
                "description": "Is the result of 'filePath' an absolute path. default: false"
              },
              "documentLink": {
                "type": "boolean",
                "description": "Create a Document Link for this find. default: true"
              },
              "allowCurrentFile": {
                "type": "boolean",
                "description": "Is a link to the current file allowed. default: false"
              },
              "languageIds": {
                "type": [
                  "array",
                  "null"
                ],
                "description": "Which languageIds this rule applies to. Use null (or omit) to apply to every language.",
                "items": {
                  "type": "string"
                }
              }
            },
            "required": [
              "pattern"
            ],
            "additionalProperties": false
          }
        },
        "custom-document-link-rules.fileroot": {
          "type": "array",
          "default": [],
          "scope": "resource",
          "description": "Root directories for the sites in this workspace folder, used to resolve 'filePath' values starting with '/'",
          "items": {
            "type": "string"
          }
        },
        "custom-document-link-rules.enableLogging": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "description": "Log resolved link targets and opened files to the console. Used for debugging."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "vsce": "^2.15.0",
    "@types/vscode": "^1.138.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
